<nav class="sideBar">
    <div class="sideNav">
        <div class="sideNav-profile medium-hidden">
            <div class="profilePicture-container">
                <div class="profilePicture-circle">
                    <img class="profilePicture" src="{{ profile.picture }}">
                </div>
                <img class="profilePicture-decoration" src="images/decorations/profilePicture/profilePicture-01.svg">
            </div>
            <div class="profile-info">
                <div class="profile-name">{{ profile.firstname }} {{ profile.lastname }}</div>
                <div class="profile-job font-mono">{{ profile.title }}</div>
            </div>
        </div>
        <div class="separator medium-hidden"></div>
        <ul class="sideNav-Links">
            {% for panel in panels %}
                <li class="sideNav-Link">
                    <div class="link-txt" href="/{{ panel.navUrl }}"><span class="medium-hidden">{{ panel.navText }}</span></div>
                    <i class="link-logo material-icons" data-color="{% if panel.colorTheme.name == 'home' %}{{ panel.colorTheme.colorLight }}{% else %}{{ panel.colorTheme.color }}{% endif %}">{{ panel.navIcon }}</i>
                    {{ navBtn.default|raw }}
                    {{ navBtn.active|raw }}
                    {{ navBtn.activeBackground|raw }}
                </li>
            {% endfor %}
        </ul>
        <div class="sideNav-contact medium-hidden">
            <div class="separator medium-hidden"></div>
            <div class="social-icons">
                <a href="{{ profile.linkedin }}" target="_blank" class="social-icon" id="linkedin"><img alt="logo de linkedin" src="/images/social_network/linkedin.png"></a>
                <a href="{{ profile.github }}" target="_blank" class="social-icon" id="git-hub"><img alt="logo de git-hub" src="/images/social_network/github.png"></a>
                <a href="{{ profile.freelanceWebsite }}" target="_blank" class="social-icon" id="hopwork"><img alt="logo de hopwork" src="/images/social_network/malt.png"></a>
            </div>
            <div class="separator medium-hidden"></div>
            <div class="copyright">CV développeur web full stack © 2017<br>Julien Lachaux -
                <a href="../public/index.php?p=legales">mentions légales</a>
            </div>
        </div>
    </div>
</nav>